# End-to-End Credit Card Default Prediction Pipeline

A machine learning pipeline designed to predict the probability of consumer credit defaults. It automatically handles raw web data streams, performs exploratory profiling, mitigates class imbalance, and executes a strictly isolated **Stratified $K$-Fold Cross-Validation** sequence to prevent data leakage.
